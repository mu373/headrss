{
  "name": "headrss",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "biome check .",
    "typecheck": "pnpm -r run typecheck",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@biomejs/biome": "^2.4.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
