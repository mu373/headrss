{
  "name": "headrss",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "biome check .",
    "typecheck": "pnpm -r run typecheck",
    "format": "biome format . --write"
  },
  "pnpm": {
    "overrides": {
      "picomatch": ">=4.0.4",
      "yaml": ">=2.8.3"
    }
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@biomejs/biome": "^2.4.10",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
